# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Mocking is done with pytest's built-in monkeypatch fixture, so no separate
# mock library is needed.
-r requirements.txt

pytest==9.1.1
